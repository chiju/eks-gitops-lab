apiVersion: v1
kind: ConfigMap
metadata:
  name: aws-cost-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  aws-cost-dashboard.json: |
{{ .Files.Get "dashboards/aws-cost-dashboard.json" | indent 4 }}
