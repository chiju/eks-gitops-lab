apiVersion: v1
kind: ConfigMap
metadata:
  name: eks-prometheus-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  eks-prometheus-dashboard.json: |
{{ .Files.Get "dashboards/eks-prometheus-dashboard.json" | indent 4 }}
